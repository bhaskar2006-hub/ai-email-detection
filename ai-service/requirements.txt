# Core
flask==3.0.3
flask-cors==4.0.1
python-dotenv==1.0.1

# Grok via OpenAI-compatible SDK (xAI uses same interface)
openai>=1.30.0

# Optional hf_local disabled for Railway deployment limits
# Deployment
gunicorn==21.2.0
